# Oracle — instructions for coding agents

The project instructions live in **`CLAUDE.md`** — read it in full before doing
anything. It applies to every agent (Claude, Codex or otherwise), including:

- one branch only: work on, commit to and push to `master`;
- every server action starts with its guard;
- the database is locked to the service-role key — never grant to `anon`;
- the owner is non-technical: plain language, British English, short replies.
