---
name: task-management-2
description: Task Management 2.0 roadmap (T1–T6) — staff IDs, conversation timeline, control centre, notifications, portal task creation, chat
metadata:
  type: project
---

# Task Management 2.0 — phased upgrade

Owner's #1 priority: turn the task page from a "form + diary" into a glanceable, conversational command centre. Design rule: advanced features must make the screen *simpler*, minimal, beautiful. Plan agreed June 2026.

- **T1 Staff IDs** — DONE (see below).
- **T2 Conversation timeline** — IN PROGRESS. Done: replies (1 level, `task_updates.parent_update_id`, migration 0041), pinned-instruction banner, chat composer with reply targeting; **@mentions** (`update_mentions` table migration 0042; `src/lib/mentions.ts` pure parse/segment helper; composer @-autocomplete from team, highlight in bubbles, server re-parses + stores). **attachments** (`task_updates.attachment_document_id`, migration 0043): a file in the composer (📎) becomes a real `documents` row (category "Attachment", owned by task's company) via `createTaskAttachment` in `lib/documents.ts`, linked to the task; rendered as a file card linking to `/api/portal/attachment?updateId=` (auth-checked, redirects to a 300s signed URL; excluded from middleware via `api/portal`). Shows on portal AND admin `/task/[code]`. NOTE: the `documents` storage bucket has a MIME allowlist — text/plain is rejected; images/PDFs fine. **inline system events** (audit_log status/deadline/priority/risk/escalation CHANGE rows → thin centered markers `ConvoEvent`, merged into the day-grouped stream by time) and **voice dictation** (VoiceButton in composer, appends transcribed text). All on PORTAL via `src/components/portal-conversation.tsx`. Reply quotes + attachments render on admin `/task/[code]`.

**T2 essentially complete.** Remaining = the admin-page conversation, which is T3 (below): reuse the SAME `portal-conversation` component, generalised to (1) interleave the audit/governance events as inline markers [already built], (2) take admin action handlers (edit/delete/any-status/correct) as props instead of the portal ones, (3) sit in a two-pane layout (conversation left, editable facts card right). One conversation engine, parameterised by capabilities + which events to interleave — same format, not a separate method.
- **IMPORTANT build note**: never import server-only modules (anything importing `@/db/supabase`) into a `"use client"` component — it bundles the service client to the browser and crashes on Vercel (happened once with staff-id). Keep pure helpers in `*-shared.ts` / `mentions.ts` (no `sb` import). Verify with: build, then grep `.next/static/chunks/*.js` for `SUPABASE_SERVICE_ROLE_KEY`.
- **T3 Glanceable control centre** — IN PROGRESS. **Done (admin task page two-pane):** `portal-conversation.tsx` generalised — actions injected as props (`addAction`/`pinAction`/`ackAction`) + capability flags (`canPin`/`canAck`/`composerHint`); portal passes portal actions, admin passes `adminAddUpdate`/`adminTogglePin` (in `task/actions.ts`, full powers: any status, reply/@mention/attachment, createdBy "web-ui"). Admin `/task/[code]` now: left = hero + the conversation (bubbles, replies, @mentions, files, voice, inline system-event markers from audit_log, pinned banner, Seen); right = facts pane (stat pills, description, source meeting, collapsible Edit form, similar tasks); below = "Full history & audit" (the OLD timeline kept intact — per-entry edit/delete/correct + filters, so nothing regressed). Grid `lg:grid-cols-[minmax(0,1fr)_22rem]`, stacks on mobile. **Done (task list — unread):** hub Tasks tab computes `TaskRow.unread` from `task_views` (viewer "admin") — a task is unread when the owner has opened it before AND `last_updated_at` is newer than their last view (i.e. someone posted after they looked). Deliberately requires a prior view so it doesn't flood on first use. Surfaced as an "Unread (N)" KPI chip (`?unread=1` filter) in `tasks-section.tsx` and an accent dot on rows (table-view + task-card). **Done (group-by):** `?group=company|status|person` on the table view — `TableView` takes a `groupBy` prop and emits section-header rows (rows pre-sorted by the group key in `tasks-section.tsx`); a "Group: None/Company/Status/Person" segmented control in the hero (table view only); Company column auto-hidden when grouped by company. T3 complete.
- **T4 Notifications** — IN PROGRESS. **Done (in-app bell):** `notifications` table (migration 0044; recipient "admin"|"person:<id>" like task_views) + `src/lib/notifications.ts` helpers (createNotification/notifyMany/notifyPinned/recipientForCreatedBy/listNotifications/unreadCount/markAllRead). Triggers wired in portalAddUpdate + adminAddUpdate (@mention → mentioned people; reply → replied-to author) and portalTogglePin + adminTogglePin (pin → task assignees + admin). `NotificationBell` client component (polls `/api/notifications` every 20s, badge count, popover list, marks read on open, links to `{to}/{code}`) in BOTH `portal-pill` (to="/portal/task") and admin `top-pill` (to="/task"). `/api/notifications` route resolves recipient from admin OR portal session; excluded from middleware (`api/notifications` in matcher). **Push-to-phone (T4b) — DONE:** `push_subscriptions` table (migration 0045; endpoint PK + recipient "admin"/"person:<id>" + p256dh/auth). `lib/push.ts` gains `addRecipientSubscription`/`removeRecipientSubscription`/`sendToRecipient`. `createNotification` now also fires `sendToRecipient` (best-effort; url `/task/<code>` for admin, `/portal/task/<code>` for persons). `/api/push/subscribe` resolves the recipient from session (admin OR portal) and stores per-recipient (admin also keeps the legacy blob for overdue alerts); excluded from middleware (`api/push`). `DevicePushToggle` (no test button) on the portal Profile; admin still uses `NotificationSettings` in Settings. Service worker `public/sw.js` already handles `{title,body,url,tag}`. NOTE: needs `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` (set) + the user grants permission on their device (can't test push delivery in the preview). The "assigned" notification now also fires (via portalCreateTask in T5).
- **NOTE:** a dev-server crash mid-compile corrupted `.next` and made ALL /api/* routes 404; fix = stop server, `rm -rf .next`, restart.
- **T5 Portal task creation** — DONE. Managers (`portal_role="manager"`) get a "+ New task" button in the portal pill (`canCreate` prop from the (app) layout) → `/portal/task/new` (manager-gated). Form picks company, accountable (me + direct reports), working people (checkboxes), priority, deadline, instruction. `portalCreateTask` action (portal/actions.ts): restricts assignees to self + `directReportIds`, company to ones those people belong to, creates the task via `insertTaskWithUniqueCodeSb` (owner_id = accountable), task_assignees with roles, instruction as first PINNED update stamped `portal-mgr:<Name>`, audit CREATE, and "assigned" notifications to assignees + admin. Staff still cannot create. Verified end-to-end. To make someone a manager: Settings → Staff portal access (or people.portal_role).
- **T6 Chat platform (later, own project)** — DM/group chat in portal; bridge = select chat message(s) → "Make this a task" (reuses Meeting extraction engine), task links back to chat. Justifies real WebSocket infra (deferred from Phase 2 sync).

Order: T1 → T2 → T3 → T4 → T5 → T6.

- **Header redesign (June 2026):** consolidated the messy 5-row / 2-card Task Management header into ONE glass Hero card. New `src/components/task-toolbar.tsx` (search · Company dropdown that filters IN PLACE via `company` param · Filters popover for Priority/Status with active badge · Show-closed toggle) replaces the old standalone `task-filters.tsx` (deleted). Company kept as a visible dropdown (owner often focuses one company); priority/status tucked into the popover. All inside the Hero, above the Focus/All toggle + KPI chips + Group control. Responsive (flex-wrap) + dark-mode verified. TODO: apply the same toolbar pattern to the staff portal task list (currently still the plain hero + tiles).

## T1 Staff IDs — shipped

Format `<companyPrefix>-<roleLetter><NN>` e.g. `CZ-E04`, `OC-AH01`, `OC-D02`.
- Letters: **D** Director (incl. C-suite: chief/CFO/CEO/CTO/COO/CIO/CMO), **AH** Admin & HR, **M** Manager, **E** Employee. Derived live from `people.role` (precedence D > AH > M > E) in `src/lib/staff-id.ts`.
- Number `NN` = rank among the company's STAFF (person_type local_staff/expat) ordered by `people.id` ascending — chronological by system entry, STABLE (id never changes, so numbers don't shift). Role change → letter changes; number unchanged.
- Company move → new prefix + new number automatically; old ID stamped into `people.previous_staff_ids` (comma list) by `updatePerson` so old refs stay traceable.
- All computed live via `getStaffIdMap()` (one query) / `staffIdFor(id)`. No stored number column.
- Shown via `<StaffIdChip>` in: people list (person-card), person drawer header, portal profile (Staff ID row), portal task team strip. `Person`/`DrawerPerson` types carry `staffId` + `previousStaffIds`.
- Migration `0040` added `people.previous_staff_ids`.
