Documents: review fixes (mobile, empty state, a11y, links)

- Mobile: expiry countdown was hidden on phones (hidden sm:block) — the key
  compliance info. Now the countdown (and "No expiry") shows on all sizes;
  full date still desktop-only.
- Empty state: first-run now shows a friendly "No documents yet" card with an
  Add button, distinct from the "no match" filter message.
- Accessibility: rows are keyboard-activatable (Enter/Space to edit).
- Integration: surface linked renewal tasks back on the document (peek shows
  "Renewal task: CODE (status)" linking to /task/<code>) — backward link
  mirroring meeting_tasks. Loaded via document_links in the page.
- Inbox "As document": cap forwarded text to 1500 chars to keep the URL safe.
- Insights: compliance-by-company bar is now a clean stacked bar (no gaps).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
